{
  "name": "tls-shipinhao-license-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute tls-license-db --file=schema.sql",
    "db:init:local": "wrangler d1 execute tls-license-db --local --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}

{
  "name": "tls-shipinhao-e2e-tauri",
  "version": "0.1.0",
  "private": true,
  "description": "Tauri 真壳 E2E（WebdriverIO + tauri-driver），故意脱离根 pnpm workspace 以避免污染主前端依赖。",
  "type": "module",
  "scripts": {
    "test": "wdio run wdio.conf.ts",
    "test:ci": "xvfb-run --auto-servernum wdio run wdio.conf.ts"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@wdio/cli": "^9",
    "@wdio/local-runner": "^9",
    "@wdio/mocha-framework": "^9",
    "@wdio/spec-reporter": "^9",
    "tsx": "^4",
    "typescript": "~5.7",
    "webdriverio": "^9"
  }
}

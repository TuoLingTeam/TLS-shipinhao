---
description: TLS-shipinhao 协作规范与编码约定
alwaysApply: true
---

## 核心原则

除非用户明确要求，否则：
- 不创建文档或测试文件
- 不编译、构建或运行程序
- 不提供冗长总结

专注于代码实现和问题解决。

## 语言

- 对话、注释、commit 信息、文档：**中文**
- 代码标识符（变量/函数/类）：**英文**
- 技术术语首次出现时附中文解释
- 使用中文标点（，。！？：；）

## 响应模式

| 我检测到 | 我的行为 |
|----|---|
| 用户给了明确指令 | **快速模式**：直接输出结论和代码 |
| 用户说"详细分析""评审""深入讨论" | **深度模式**：展开多维度分析 |
| 需求模糊或有多种理解方式 | **澄清模式**：先复述我的理解 + 提问确认 |
| bug 可复现但原因不明 | 主动建议切换 Debug Mode |

## 代码风格

Rust 代码必须遵循：
- 标准 Rust 命名规范（snake_case 函数/变量，PascalCase 类型/trait）
- 使用 `tracing` 而非 `println!` 记录日志（release 模式下 `release_max_level_off`）
- 敏感字符串用 `obfstr` 编译期加密
- 异常处理用 `anyhow::Result` / `thiserror`，避免裸 `unwrap()`

TypeScript / Vue 代码必须遵循：
- 使用严格类型，优先消除裸 `any`
- Vue 3 Composition API + `<script setup>` 优先
- 业务逻辑收敛到 composable / store，模板保持简洁
- Tailwind CSS v4 优先，避免内联 style

### 编码约束

| 约束 | 阈值 |
|---|---|
| 单个函数/方法 | ≤ 50 行 |
| 单个文件 | ≤ 1000 行 |
| 嵌套层级 | ≤ 3 层 |
| 函数参数 | ≤ 5 个 |
| 注释 | 解释 WHY 而非 WHAT |

**过度设计判断**：
- 当前只有 1 个实现 → 不建接口/抽象工厂
- 当前没有变化需求 → 不用策略模式/模板方法
- 消除重复会引入更高复杂度 → 保留适度重复

## 工作流程

1. 明确需求 - 不清楚时主动提问，避免过度设计
2. 查看现有代码 - 理解项目结构和实现模式
3. 最小化实现 - 只编写必要代码，避免冗余
4. 自验 - 主动运行 lint 检查语法和类型错误

### 模式选择

| 我判断到 | 我的行为 |
|----|---|
| 改动 ≤ 3 个文件，逻辑清晰 | 直接执行 |
| 改动 > 3 个文件，或需要设计决策 | 先说方案 + 理由，确认后执行 |
| 跨多模块/服务，有架构影响 | 主动建议切换 Plan Mode |
| 破坏性操作（删文件/改架构） | 必须先确认 |

### 执行后

1. 主动运行 lint 检查
2. 前端改动时主动验证 UI
3. 列出改了哪些文件、有无需要注意的

### 对话健康监控

- 当前对话已完成一个完整功能：主动告知可开新对话
- 回答变得模糊或重复：主动建议开新对话
- 用户话题切换到完全不同功能：主动建议开新对话

## 技术要点

处理此项目时注意：
- Tauri 2 桌面端：`apps/desktop/`（Rust 命令）+ `apps/ui/`（Vue 3 前端）
- Rust workspace 依赖在根 `Cargo.toml` 统一管理，共享 crate 在 `crates/`
- 前端打包后会经 `javascript-obfuscator` 混淆（配置在 `scripts/obfuscator.config.json`）
- Windows WebView2（Chromium）与 Mac WKWebView 行为差异需注意（CSP、URL 规范化等）
- 授权 API 域名在 `apps/desktop/src/app_settings.rs` 管理，用 `obfstr` 加密
- Worker 部署入口在 `backend/wrangler.toml`，用 `wrangler deploy`
- 发版流程：`pnpm release:build:deep` 或推 `v*` tag 触发 CI

## 决策思维

遇到设计决策时，从以下维度审视：

| 维度 | 我问自己 |
|---|-----|
| 安全 | 有新攻击面吗？输入已验证？权限已检查？ |
| 架构 | 职责对吗？耦合度可接受吗？ |
| 性能 | 数据量大时会慢吗？需要缓存/分页吗？ |
| UX | 错误提示友好吗？加载状态有吗？ |
| 测试 | 关键路径覆盖了吗？边界条件想到了吗？ |

## MCP 工具与技能使用

| 工具/技能 | 使用场景 |
|---|----|
| Context7 | 不确定 Tauri/Vue/Cloudflare Workers API 用法时主动查 |
| Sequential Thinking | 遇到 ≥2 候选方案、复杂 bug、架构决策时 |
| Browser | 前端改动后主动验证 UI |
| `vue-engineer` | Vue 3 组件开发、Composable 设计、Pinia 状态管理 |
| `cloudflare-deploy` | Worker 部署、Wrangler 配置、D1 操作 |
| `code-review` / `security-review` | 代码审查、安全审计 |
| `system-architect` | 跨 crate 架构决策、API 设计 |
| `ui-ux-pro-max` | UI/UX 设计改进 |
| `yeet` | 一键 commit + push + PR |
| `bash` | Shell 脚本编写与审查 |

## Forgevia 结构化开发工作流

用户明确说 `Forgevia <命令>` 时，读取对应技能（`~/.codex/skills/forgevia*/SKILL.md`）并按流程执行。**不自动触发**。

| 命令 | 用途 | 触发时机 |
|---|---|----|
| `Forgevia init` | 初始化项目 OpenSpec 环境 | 新项目首次使用 |
| `Forgevia draw` | 生成 Mermaid 时序图/架构图 | 功能设计可视化 |
| `Forgevia think` | 梳理和澄清需求 | 需求模糊、开发前 |
| `Forgevia propose` | 将需求转为结构化变更提案 | think 确认后或需求已明确 |
| `Forgevia implement <change>` | 按任务驱动执行开发 | 提案创建后 |
| `Forgevia review` | 代码评审检查点 | 关键节点、合并前 |
| `Forgevia verify-web` | 浏览器验证 Web 行为 | 涉及页面/交互改动 |
| `Forgevia tasks` | 查看未完成任务 | 随时 |
| `Forgevia archive <change>` | 归档已完成变更 | 开发+评审+验证完成后 |
| `Forgevia doctor` / `repair` | 检查/修复环境 | 环境异常时 |

标准流程：`draw → think → propose → implement → review → verify-web（如需要）→ archive`

大型任务优先使用 Forgevia 流程，它自带需求梳理、任务拆分、评审和归档。

## 信息呈现

- 改动多时：表格列出"文件 → 改了什么"
- 方案对比时：对比表
- 报错分析时：先给结论和修复方案，再解释原因

## 沟通方式

- 使用中文交流和编写注释
- 简洁、技术导向的回答
- 遇到歧义主动澄清
- 避免重复说明已完成的操作

## 禁止

### 代码
- ❌ 硬编码 secrets / tokens / API keys
- ❌ 提交不完整的功能
- ❌ 跳过验证直接说"完成了"
- ❌ 单文件超 1000 行不拆分
- ❌ 单个函数超 50 行不拆分
- ❌ 噪声注释

### 行为
- ❌ 不确认就执行破坏性操作
- ❌ 不知道就猜——先搜索代码库或问用户
- ❌ 创建版本文件（v1/v2）、临时文件（temp/draft）、备份文件
- ❌ 过度设计
- ❌ 说完方案不等确认就动手改
- ❌ 长对话已失焦还不主动建议开新对话

---

## 项目目录说明

Tauri 2 桌面应用 + Cloudflare Worker 授权后端的 Rust monorepo：

| 目录 | 性质 | 说明 |
|---|---|---|
| `apps/desktop/` | **桌面端源码** | Tauri 2 Rust 命令、适配器、状态管理、图标 |
| `apps/ui/` | **前端源码** | Vue 3 + Vite + Pinia + Tailwind CSS v4 |
| `crates/` | **共享 Rust 库** | `domain-core`（领域模型）、`desktop-services`（业务服务）、`security-core`（设备指纹/授权校验） |
| `backend/` | **Worker 后端** | `license-worker`（CF Worker）、`license-service`（授权域逻辑）、`api-contracts`（共享契约）、`wrangler.toml` |
| `scripts/` | **构建脚本** | `build-obfuscate.mjs`（混淆发版）、`obfuscator.config.json`、`tauri_dev.sh` |
| `tools/` | **辅助工具** | `build-tools`、`xtask`（发版/性能辅助） |
| `backup/` | **历史备份（只读）** | 旧版 Python 客户端资产，不参与主线 |
| `TLS-shipinhao-release/` | **构建产物（禁止编辑）** | 由 `build-obfuscate.mjs` 自动生成的混淆镜像，手动修改会被覆盖 |

### ⚠️ 强制约束

- **严禁直接修改 `TLS-shipinhao-release/` 中的任何文件**——该目录是构建产物
- 发版时修改源码目录，推 `v*` tag 触发 CI 或本地 `pnpm release:build:deep`
- Worker 部署与桌面发版独立：`cd backend && wrangler deploy`

---

## 沙箱受限目录

以下目录在 `.gitignore` 中被忽略，Cursor 沙箱默认无法通过 Read / Grep / Glob 直接访问：

```
logs/    docs/    dist/
.cursor/ .codex/  .kiro/  .vscode/  .windsurf/
```

**当用户要求读取或分析上述目录中的文件时，我必须：**
1. 使用 Shell 工具并附加 `required_permissions: ["all"]` 来读取文件内容
2. 不要尝试用 Read / Grep 等工具直接读取（会被沙箱拦截或返回空）
3. 典型场景：用户让我分析 `logs/` 下的日志文件、查看 `docs/` 文档、检查 `dist/` 构建产物